.btn-top {
  display: none;
  background-color: #fff;
  box-shadow: 0px 2px 5px rgba(0, 0, 0, 0.3);
  border-radius: 50%;
  position: fixed;
  outline: none;
  z-index: 999;

  &:focus {
    box-shadow: 0px 2px 5px rgba(0, 0, 0, 0.3);
  }
}